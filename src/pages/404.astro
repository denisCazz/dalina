---
import Layout from '../layouts/Layout.astro';
import Shell from '../components/Shell.astro';
import { seo, site } from '../data/site';

export const prerender = true;
---

<Layout
  title={seo.notFoundTitle}
  description={`La pagina non esiste. Torna su ${site.name}, ristorante e pizzeria a Sommariva del Bosco.`}
  noindex
>
  <Shell>
    <section class="wrap page">
      <p class="kicker">404</p>
      <h1>Qui non cՏ nemmeno la pizza.</h1>
      <p class="muted">La pagina non cՏ. Torna al forno, o al menu.</p>
      <div class="actions">
        <a class="btn" href="/">Home</a>
        <a class="btn btn-ghost" href="/menu">Menu</a>
      </div>
    </section>
  </Shell>
</Layout>

<style>
  .page {
    padding: 6rem 0;
    min-height: 60vh;
  }

  h1 {
    font-size: clamp(2.8rem, 8vw, 5rem);
    max-width: 12ch;
    margin: 0.6rem 0 1rem;
  }

  .actions {
    display: flex;
    gap: 0.8rem;
    margin-top: 2rem;
  }
</style>
